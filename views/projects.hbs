<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{pageTitle}}</title>
    </head>
    <body>
        {{> header}}
        <ul>
            {{#each projects}}
                <li>{{this.domain}}: {{this.name}}</li>
            {{/each}}
        </ul>
        {{> footer}}
    </body>
</html>